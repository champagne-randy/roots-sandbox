extends layout

block content
	- var arr = ['<a>','<b>','<c>']
	ul
		- for (var i=0; i< arr.length; i++)
			li
				span= i+1
				span!= " - unescaped: " + arr[i] +" vs. "
				span= "escaped: " + arr[i]